# Changesets

This folder holds the changesets that drive releases. A changeset is a small
markdown file describing a change and the version bump it deserves.

Run `pnpm changeset` after making a change, pick the packages it affects and the
bump type, and commit the generated file alongside your code.

See the [changesets docs](https://github.com/changesets/changesets) for the full
format.
