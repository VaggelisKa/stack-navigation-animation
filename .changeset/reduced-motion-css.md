---
"@stacknav/core": patch
---

Disable browser-owned stack transitions when `prefers-reduced-motion: reduce` is active, including when the preference changes during an animation or an inline duration has already been applied.
