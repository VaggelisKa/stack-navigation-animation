---
---

Demo-only: both demos now start in the default browser swipe-back mode, so the
Angular README no longer claims they opt into custom. No published behavior
changes; the pending swipe-back-modes bump already covers the feature.
